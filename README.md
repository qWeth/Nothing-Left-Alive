# Nothing-Left-Alive
2D roguelike game built with C++ and Godot

## Установка зависимостей

### Ubuntu / Debian
```bash
sudo apt update
sudo apt install cmake libglfw3-dev libglew-dev godot
```

### Arch
```bash
sudo pacman -Syu
sudo pacman -S cmake godot
```
